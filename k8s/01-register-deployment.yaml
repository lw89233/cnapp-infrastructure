apiVersion: apps/v1
kind: Deployment
metadata:
  name: register-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: register-service
  template:
    metadata:
      labels:
        app: register-service
    spec:
      initContainers:
      - name: wait-for-mysql
        image: busybox:1.28
        command: ['sh', '-c', 'until nc -z -w 5 mysql-service 3306; do echo "Czekam na bazę danych..."; sleep 2; done;']
      containers:
      - name: register-container
        image: lw89233/register:latest
        ports:
        - containerPort: 8000
        env:
        - name: REGISTRATION_MICROSERVICE_PORT
          value: "8000"
        - name: DB_HOST
          value: "mysql-service"
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: mysql-configmap
              key: MYSQL_DATABASE
        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: mysql-configmap
              key: MYSQL_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_PASSWORD
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: register-service
spec:
  selector:
    app: register-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000